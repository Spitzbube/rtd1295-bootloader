#ifndef _TARGET_H_
#define _TARGET_H_

#define timer_init(mode) \
do{}while(0)

#define timer_start() \
do{}while(0)

#define timer_stop() \
do{}while(0)

#define Vax_caculate() \
do{}while(0)

static inline int
timer_get(int timer_id)
{
  return 0;
}

#define start_hook() \
do{}while(0)

void inline
stop_hook(void)
{
}

void inline
msg_hook(int n_run)
{
}
#endif /* _TARGET_H_ */
